/**
 * `inline` — render pickers in the document flow (use inside Vaul drawer / stacking contexts).
 * `popover` — default Radix popovers / select portaled to body.
 */
export type BookingPickerLayout = "popover" | "inline";
